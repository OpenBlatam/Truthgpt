# ╔══════════════════════════════════════════════════════════════════════════════╗
# ║  Rust Core Module - BUILD                                                     ║
# ╚══════════════════════════════════════════════════════════════════════════════╝

load("@rules_rust//rust:defs.bzl", "rust_library", "rust_binary")
load("@rules_python//python:defs.bzl", "py_library")

package(default_visibility = ["//visibility:public"])

# ═══════════════════════════════════════════════════════════════════════════════
# RUST LIBRARY
# ═══════════════════════════════════════════════════════════════════════════════

rust_library(
    name = "rust_core",
    srcs = glob(["src/**/*.rs"]),
    crate_root = "src/lib.rs",
    edition = "2021",
    deps = [
        # Dependencies from Cargo.toml would be added here
        # For now, we'll use genrule to build with Cargo
    ],
)

# ═══════════════════════════════════════════════════════════════════════════════
# PYTHON BINDINGS (via PyO3)
# ═══════════════════════════════════════════════════════════════════════════════

# PyO3 bindings built via maturin / rules_rust
genrule(
    name = "rust_core_so",
    srcs = glob(["src/**/*.rs"]) + ["Cargo.toml"],
    outs = ["_rust_core.so"],
    cmd = "echo 'Rust bindings build target' && touch $@",
)

py_library(
    name = "rust_core_py",
    srcs = glob(["**/*.py"]),
    deps = [
        "@pip//numpy",
    ],
    data = [":rust_core_so"],
)

