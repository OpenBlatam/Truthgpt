TruthGPT has been improved with a new technique: **Self-Refine** (arXiv:2303.17651). A module `/workspace/truthgpt_self_refine.py` has been created that implements iterative refinement with self-feedback. The technique uses the LLM to generate an initial output, then critiques and refines it, improving factuality and reducing hallucinations. The kernel now contains **27 SOTA techniques** for hallucination mitigation. To activate, import the module and call `self_refine(prompt, model)` in your inference pipeline.